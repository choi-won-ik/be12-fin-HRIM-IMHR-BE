apiVersion: batch/v1
kind: CronJob
metadata:
  name: score-batch-job
  namespace: imhr
spec:
  schedule: "0,10,20,30,40,50 * * * *"
  startingDeadlineSeconds: 300
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: score-batch
              image: imhr/score-batch1.16
              env:
                - name: BATCH_JOB_NAME
                  value: scoreJob
              envFrom:
                - configMapRef:
                    name: back-cm
          restartPolicy: OnFailure
      backoffLimit: 5
